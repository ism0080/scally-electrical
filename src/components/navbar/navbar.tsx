import { Box } from '@chakra-ui/react'
import Image from 'next/image'

export const NavBar = () => {
  return (
    <Box padding='20px'>
      <img src='/logo.svg' width='200px' height='100%'/>
    </Box>
  )
}
